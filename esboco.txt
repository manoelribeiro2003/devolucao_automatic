// Na planilha, ler qual o proximo registro de devolucao
$codigo = openpyxl.ler_celula(linhaXcoluna)
$nome = openpyxl.ler_celula(linhaXcoluna)
$lote = openpyxl.ler_celula(linhaXcoluna)
$quantidade = openpyxl.ler_celula(linhaXcoluna)


// Enviar os dados p/ selenium  // Executar a devolucao no navegador
wd = webdriver.Firefox()

selectByID('<campo do codigo do produto>').insetvalues($codigo)
Inset_key.(Return)

selectByDropdownButton('<dropdown>')

if(selectByXPATH | selectByID | selectByName.value == $lote){
    selectByXPATH | selectByID. insertValues($quantidade)
    Inset_key.(Return)

    if(selectByXPATH | selectByID. insertValues($quantidade) == 0|NULL){
        // Marcar na planilha o registro devolvido
        $resultado = openpyxl.escrever_celula(linhaXcoluna, "ok")
    }else{
        $resultado = openpyxl.escrever_celula(linhaXcoluna, "erro")
    }
}
linha = 1;
celula = "'A{linha}'"

while (controle == TRUE):
    if celula == NULL:
        sys.exit()
    else:
        variavel1 = sheet[celula]
        linha++


